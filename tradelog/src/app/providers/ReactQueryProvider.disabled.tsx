"use client";

// TODO: Re-enable this provider once npm registry access is restored and
// `@tanstack/react-query` is installed successfully.
export function ReactQueryProvider({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
