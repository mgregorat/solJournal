import { solana, solanaTestnet, solanaDevnet } from '@web3modal/solana/chains';
export declare const initializeWalletConnect: (props: {
    chains: Array<typeof solana | typeof solanaTestnet | typeof solanaDevnet>;
    projectId: string;
    metadata: {
        name: string;
        description: string;
        url: string;
        icons: Array<string>;
    };
}) => void;
